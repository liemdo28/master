# Phase 13 Observability

## Implemented
- Added `/api/enterprise/health`.
- Reports Postgres, MinIO, Qdrant, provider router, memory router, browser router, and queue stats.

## Required Next
Expose this status in the Mi-Core Dashboard and add active probes for connectors, agents, provider latency, browser workers, and dead-letter jobs.
