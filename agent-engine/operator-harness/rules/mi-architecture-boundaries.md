# mi-architecture-boundaries

- Preserve Mi's layer model from `MI_MASTER_ARCHITECTURE.md`.
- Do not couple UI directly to local-agent internals when server routes should own the boundary.
- Do not bypass `agent-engine` safety components for autonomous coding changes.
- Keep connector state under documented data/cache paths.
- Treat owner memory, consent, and business automation as first-class architecture constraints.

