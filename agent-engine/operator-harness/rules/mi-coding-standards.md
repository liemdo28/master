# mi-coding-standards

- Match the existing JavaScript/TypeScript style in the touched package.
- Prefer explicit module boundaries over broad utility files.
- Validate inputs at API and connector boundaries.
- Avoid hidden background behavior unless there is a health signal and audit trail.
- Do not add dependencies when the platform or existing package already provides a reasonable solution.

