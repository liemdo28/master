# mi-test-evidence

- Every meaningful change needs evidence: command output, smoke result, screenshot, API response, or report.
- If verification cannot run, state the exact blocker and residual risk.
- Do not call a workflow complete based only on static inspection when runtime behavior is affected.

