# mi-remote-control-safety

- Remote command execution requires trusted device and active authenticated session.
- Remote write/delete/deploy/shell actions are Level 3 by default.
- Health and discovery responses must not leak credentials, local secrets, or private files.
- Every remote command needs timeout and audit trail.

