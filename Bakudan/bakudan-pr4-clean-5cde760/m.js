const fs=require("fs");const lines=fs.readFileSync("api/index.php","utf8").split("\\n");const out=[];for(let i=0;i<lines.length;i++){const l=lines[i];out.push(l);if(i===365&&l.trim()==="}"){out.push("","// v3: rename staff-training-videos to staff-training for .htaccess","function migrate_staff_training_v3(SQLite3 $db): void {",'    if ($db->querySingle("SELECT value FROM settings WHERE key='migration_staff_training_v3'") === '1') return;"    $oldSlug = 'staff-training-videos';",'    $newSlug = 'staff-training';