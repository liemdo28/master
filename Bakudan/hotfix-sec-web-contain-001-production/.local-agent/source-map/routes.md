# Routes

Total: 406 routes

### index.php
- `GET /login` → controller
- `GET /register` → controller
- `GET /logout` → controller
- `GET /home` → controller
- `GET /calendar` → controller
- `GET /inbox` → controller
- `GET /my-tasks` → controller
- `GET /related-tasks` → controller
- `GET /new-tasks` → controller
- `GET /overview` → controller
- `GET /ceo` → controller
- `GET /team` → controller
- `GET /team-load` → controller
- `GET /api/team/summary` → controller
- `GET /api/team/members` → controller
- `GET /api/team/rebalance-plan` → controller
- `GET /api/team/rebalance-apply` → controller
- `GET /api/command-palette` → controller
- `GET /api/sidebar/badges` → controller
- `GET /api/team/rebalance-status` → controller
- `GET /api/verification/summary` → controller
- `GET /api/accounting/verification/summary` → controller
- `GET /api/decision/problems` → controller
- `GET /api/decision/compare` → controller
- `GET /api/decision` → controller
- `GET /api/decision/next` → controller
- `GET /api/decision/simulate` → controller
- `GET /api/autonomy/decisions` → controller
- `GET /api/autonomy/approve` → controller
- `GET /api/autonomy/reject` → controller
- `GET /api/autonomy/execute-safe` → controller
- `GET /api/autonomy/log` → controller
- `GET /api/autonomy/settings` → controller
- `GET /ai-import` → controller
- `GET /ai-import/store` → controller
- `GET /ai-import/store/create` → controller
- `GET /ai-import/bills` → controller
- `GET /ai-import/bills/store/create` → controller
- `GET /settings` → controller
- `GET /admin/penalty` → controller
- `GET /admin/verification-rules-preview` → controller
- `GET /admin/verification-rules` → controller
- `GET /admin/penalty/add` → controller
- `GET /admin/penalty/update` → controller
- `GET /admin/penalty/remove` → controller
- `GET /admin/penalty/toggle` → controller
- `GET /api/admin/penalty/summary` → controller
- `GET /api/penalty/my-summary` → controller
- `GET /admin/extensions` → controller
- `GET /api/extensions/pending-count` → controller
- `GET /webhook/telegram` → controller
- `GET /telegram/link/init` → controller
- `GET /telegram/link/unlink` → controller
- `GET /telegram/preferences` → controller
- `GET /telegram/setup-webhook` → controller
- `GET /api/notifications` → controller
- `GET /api/notifications/read-all` → controller
- `GET /api/calendar` → controller
- `GET /api/my-tasks/next-focus` → controller
- `GET /api/tasks/bulk` → controller
- `GET /api/users/assignable` → controller
- `GET /api/search` → controller
- `GET /api/tasks/reassign/preview` → controller
- `GET /api/tasks/reassign` → controller
- `GET /bills` → controller
- `GET /bills/templates` → controller
- `GET /bills/templates/create` → controller
- `GET /bills/store/create` → controller
- `GET /bills/create` → controller
- `GET /projects` → controller
- `GET /tasks` → controller
- `GET /tasks/reorder` → controller
- `GET /admin/projects/merge` → controller
- `GET /admin/tasks/bulk-complete` → controller
- `GET /admin/tasks/classify-by-store` → controller
- `GET /admin/users` → controller
- `GET /admin/users/create` → controller
- `GET /admin/stores` → controller
- `GET /admin/store-command` → controller
- `GET /admin/vendors` → controller
- `GET /asana` → controller
- `GET /asana/workspaces` → controller
- `GET /asana/save` → controller
- `GET /asana/sync` → controller
- `GET /asana/disconnect` → controller
- `GET /asana/settings` → controller
- `GET /asana/migrate` → controller
- `GET /admin/projects/assign-stores` → controller
- `GET /admin/data-hygiene` → controller
- `GET /admin/cleanup/overdue` → controller
- `GET /admin/bills/convert-projects` → controller
- `GET /admin/stores/merge` → controller
- `GET /admin/releases` → controller
- `GET /admin/releases/create` → controller
- `GET /admin/releases/create` → controller
- `GET /api/admin/releases/stats` → controller
- `GET /api/admin/releases/freeze` → controller
- `GET /api/releases/process-scheduled` → controller
- `GET /admin/incidents` → controller
- `GET /admin/payroll` → controller
- `GET /admin/payroll/create` → controller
- `GET /admin/payroll` → controller
- `GET /admin/payroll/mark-paid` → controller
- `GET /api/payroll/stats` → controller
- `GET /admin/incidents/create` → controller
- `GET /admin/incidents` → controller
- `GET /admin/org-chart` → controller
- `GET /ceo/scorecard` → controller
- `GET /admin/benchmarks` → controller
- `GET /admin/goals` → controller
- `GET /admin/goals/create` → controller
- `GET /admin/budget` → controller
- `GET /admin/budget/create` → controller
- `GET /api/franchise/regions` → controller
- `GET /api/franchise/regions` → controller
- `GET /api/franchise/districts` → controller
- `GET /api/franchise/districts` → controller
- `GET /api/franchise/hierarchy` → controller
- `GET /api/franchise/org-chart` → controller
- `GET /api/franchise/scorecard` → controller
- `GET /api/franchise/benchmarks` → controller
- `GET /api/franchise/kpi/snapshot` → controller
- `GET /bills/seed-raw-stockton` → controller
- `GET /api/tasks/reorder` → controller
- `GET /deadline-extensions/history` → controller
- `GET /admin/deadline-extensions` → controller
- `GET /api/deadline-extensions/request` → controller
- `GET /api/deadline-extensions/quota` → controller
- `GET /api/admin/deadline-extensions/pending` → controller
- `GET /admin/tasks` → controller
- `GET /admin/tasks/all` → controller
- `GET /admin/tasks/by-store` → controller
- `GET /admin/tasks/duplicates` → controller
- `GET /admin/tasks/duplicates/group` → controller
- `GET /admin/tasks/duplicates/archive` → controller
- `GET /admin/tasks/duplicates/merge` → controller
- `GET /admin/tasks/workflow` → controller
- `GET /admin/tasks/workflow/check` → controller
- `GET /admin/tasks/workflow/accept` → controller
- `GET /admin/tasks/workflow/reject` → controller
- `GET /admin/tasks/workflow/reopen` → controller
- `GET /api/admin/penalty-config` → controller
- `GET /api/admin/penalty-config` → controller
- `GET /migrate.php` → controller
- `GET /admin_complete_overdue.php` → controller
- `GET /webhooks/telegram` → controller
- `GET /settings/telegram` → controller
- `GET /api/telegram/connect/init` → controller
- `GET /api/telegram/disconnect` → controller
- `GET /api/telegram/status` → controller
- `GET /api/telegram/test-send` → controller
- `GET /api/telegram/set-webhook` → controller
- `GET /api/telegram/webhook-info` → controller
- `GET /api/telegram/jobs/due-today` → controller
- `GET /api/telegram/jobs/overdue` → controller
- `GET /api/email/jobs/due-today` → controller
- `GET /api/email/jobs/overdue` → controller
- `GET /api/email/jobs/process-queue` → controller
- `GET /admin/command-center` → controller
- `GET /admin/command-center/predictions` → controller
- `GET /admin/command-center/predictions/run` → controller
- `GET /admin/command-center/recommendations` → controller
- `GET /admin/command-center/scores` → controller
- `GET /admin/command-center/workflows` → controller
- `GET /admin/command-center/workflows` → controller
- `GET /admin/command-center/notifications` → controller
- `GET /admin/command-center/simulations` → controller
- `GET /admin/command-center/simulate` → controller
- `GET /admin/command-center/memory` → controller
- `GET /admin/command-center/ai-decisions` → controller
- `GET /admin/command-center/api/summary` → controller
- `GET /ceo/war-room` → controller
- `GET /ceo/war-room` → controller
- `GET /operations/today` → controller
- `GET /manager/command` → controller
- `GET /action-center` → controller
- `GET /store/checklist/open` → controller
- `GET /store/checklist/open/submit` → controller
- `GET /store/checklist/close` → controller
- `GET /store/checklist/close/submit` → controller
- `GET /store/checklist/history` → controller
- `GET /company/calendar` → controller
- `GET /my-day` → controller
- `GET /control-tower` → controller
- `GET /playbooks` → controller
- `GET /admin/release-dashboard` → controller
- `GET /admin/shifts` → controller
- `GET /admin/shifts` → controller
- `GET /admin/employees` → controller
- `GET /admin/training` → controller
- `GET /admin/procurement` → controller
- `GET /admin/documents` → controller
- `GET /admin/compliance` → controller
- `GET /ceo/boardroom` → controller
- `GET /admin/digital-twin` → controller
- `GET /search` → controller
- `GET /api/search/full` → controller
- `GET /activity` → controller
- `GET /api/activity` → controller
- `GET /notifications` → controller

