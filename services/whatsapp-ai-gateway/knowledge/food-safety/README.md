# Food Safety Knowledge

Structured source for food safety rules, thresholds, and corrective actions.

`default-rules.json` is the offline fallback used when the Google Sheet cannot be fetched or does not parse into a complete rule set.
