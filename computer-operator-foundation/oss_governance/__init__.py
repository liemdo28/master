# Open Source Governance — Phase 0.5
"""
Mi treats open source projects like employees.

Registry, Scorecard, Lifecycle Engine, and Dashboard.
Recruits from discovery → audit → ROI → architecture → pilot →
production → maintenance → retirement.

CTO Rule: Only READ-ONLY evaluations, never write to upstream projects,
never fabricate adoption metrics or test coverage numbers.
"""
__version__ = "0.1.0"
