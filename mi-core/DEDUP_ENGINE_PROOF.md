# Dedup Engine Proof

Runtime function: `buildDedupEngineProof()`.

The proof reports:
- task count
- duplicate pair count
- duplicate candidates for review

Duplicate candidates are not auto-deleted; they are surfaced for owner review.
