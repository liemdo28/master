# mi-whatsapp-contracts

- WhatsApp routes must return stable JSON shapes for success and failure.
- Never expose API keys, pairing data, session data, or raw provider secrets.
- Message send actions require clear target identity and audit logging.
- Gateway-offline behavior must be explicit and recoverable.

