# Dependency Graph Proof

Runtime function: `buildDependencyGraphProof()`.

The proof reports:
- task count
- dependency edge count
- topological order
- cycle status

Passing condition: no dependency cycle.
