# Repo Cleanliness Audit

Date: 2026-06-28 · Branch base: `master` @ `4f1929c3`

## Commands run
```
git status --short
git submodule status --recursive
git diff --check
git ls-files | grep -E "(\.sqlite|\.db|\.log|\.env|cookies|session|token|secret)"
```

## Results

### `git status --short` — 10 modified, 20 untracked
| Class | Files | Disposition |
|---|---|---|
| **source change** | 18 new files (9 source certs, functional/mapping/OSS/workflow/duplicate reports, 4 new tests, OSS manifest) | ✅ commit in this PR |
| **runtime artifact (dirty, uncommitted)** | `services/doordash-agent/data/sessions/*.json` + `*.png` (7), `services/doordash-agent/data/latest-metrics.json`, `reports/evidence/knowledge-rate-limit/runtime-429-audit.jsonl` | ⚠️ generated by runtime processes; **not** part of this PR. Should be `.gitignore`d or committed by their owning service. |
| **must investigate/remove** | `d` (untracked, repo root, 5.4 KB) | ⚠️ stray file (looks like an accidental shell redirect). Not introduced by this work. |

### `git diff --check`
✅ No whitespace or merge-conflict markers. (CRLF/LF warnings only — `core.autocrlf=true`, no `.gitattributes`.)

### `git submodule status --recursive`
✅ Empty — no submodules, no broken gitlinks.

### Tracked DB / secret scan
| Finding | Class | Action |
|---|---|---|
| `mi-core/data/qb-agent.db` (+3 `.backup-*`), `data/qb-agent.db.backup-*`, `Agent/data/qb-agent.db` | **tracked runtime DB — must remove** | ⚠️ Violates the "no tracked runtime DB" rule. **Pre-existing**, not from PR #24. Recommend `git rm --cached` + `.gitignore` in a dedicated cleanup PR (not mixed into this audit). |
| `mi-core/evidence/**/-redacted.{txt,log}` | redacted evidence | ✅ acceptable (already redacted) |
| `services/qb-ops-agent/dist/security/token.js.map` | compiled sourcemap (`token.ts` module) | ✅ not a secret; `dist` build artifact |
| New audit files (tests + reports + manifest) | source | ✅ scanned — no `api_key`/`password`/`bearer`/`AKIA`/PEM patterns. Test fixtures use obvious fake tokens. |

## Verdict
**REPO CLEAN = PARTIAL.** Per the CTO truth rule, the repo is **not** claimed clean while:
1. tracked runtime DBs (`qb-agent.db` + backups) remain tracked, and
2. uncommitted runtime artifacts + a stray `d` file sit in the working tree.

None of these were introduced by PR #24 or this audit (all pre-existing). They are classified above with a remediation path. This PR commits **only** source (reports + tests + manifest) and leaves the pre-existing artifacts untouched rather than bundling a risky history-rewrite into a certification PR.
