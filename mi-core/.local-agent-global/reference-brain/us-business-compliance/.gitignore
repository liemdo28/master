# US Business Compliance DB — generated content
*.md
*.json
*.csv

# Keep source files
!*.py
!.gitignore
!reports/*.md
!source-catalog/*.md

# Keep only the pipeline, reports, and manifest
# Content is regeneratable with: python __build_fix__.py && python expand_content.py
