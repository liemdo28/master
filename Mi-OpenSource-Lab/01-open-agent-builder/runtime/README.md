# Open Agent Builder Runtime

This folder is reserved for the guarded lab clone/install.

Do not commit:
- `node_modules`
- build output
- `.env`
- generated browser data

Install only after license verification.
