# WebLLM Runtime

This folder is reserved for an approved WebLLM lab install.

Do not commit:
- `node_modules`
- browser caches
- model cache
- build output

The static fallback demo is in `pocs/webllm-dashboard-demo`.
